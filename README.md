Run 
    python3 -m http.server 8000

From the root directory (where src is) to start a local server.
Then navigate to http://localhost:8000.

If it is not working, please go to DevTools -> Network -> Disable Cache and Reload the Site.